Placeholder. Real screenshots and GIFs (e.g. captured from iTerm2 or
Kitty) will land here once the design review pass is done. Until
then the README ships ASCII screenshots generated by the smoke
renderer (`cargo run --example smoke`), which match the production
binary 1:1.
